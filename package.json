{
  "name": "origami-duel",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json --noEmit && vite build",
    "preview": "vite preview",
    "gen-sprites": "node scripts/gen-placeholders.mjs",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["canvas", "esbuild"]
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "canvas": "^3.0.0",
    "tsx": "^4.19.0",
    "typescript": "^6.0.0",
    "vite": "^6.0.0",
    "vite-tsconfig-paths": "^5.1.0"
  }
}
